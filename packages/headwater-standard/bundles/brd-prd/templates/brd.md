---
id: "{{ACME-BRD-self-serve-onboarding, minted once and never reissued}}"
status: draft
status_since: "{{today}}"
last_verified: "{{today}}"
summary: "{{one sentence a reader scans, which is the scent and not the title}}"
requirement_tier: brd
provenance:
  warrant: accepted
  agency: "{{human | agent | mixed}}"
  accepted_by: "{{a human, always named}}"
  evidence_basis: "{{evidenced | reconstructed | unevidenced}}"
relations:
  elaborated_by:
    - "{{the identifier of each product requirements document that answers this one. Absent until one is written, and the expectation reports its absence after 90 days}}"
---

# {{the initiative, named for the outcome and not for the solution}}

## Business need

{{The need, stated for the sponsor who pays for the work. Name who has the problem, what it costs them today, and what happens if nobody acts. Write the need and never the solution. A business need that names a technology has already decided the product requirements document below it.}}

{{Name the boundary. An initiative whose need reaches three products is three initiatives that nobody split.}}

## Business requirements

{{What the business requires of any solution, numbered, one requirement per line. Each one is a statement about the outcome rather than about the implementation. A reader must be able to hold each line against a candidate solution and say whether it holds.}}

{{Nothing here reaches a requirement inside this heading. Identity is per document, so no edge names line 3, and the doctrine of this entry carries the finding. Number the lines anyway, because the product requirements document cites them as prose and a person reads the citation.}}

## Success measures

{{How a reader establishes, after the work ships, that the need above was met. Name the measure, the current value and the value that counts as success. A measure with no current value is a target nobody can tell from where the business already is.}}

{{This heading is what separates a requirement from a wish. No rule reads what is written under it, and `OB-BP-3` records why.}}
