---
id: "{{HW-DR-0001, minted once and never reissued}}"
title: "{{the decision, stated as a decision and not as a topic}}"
status: draft
status_since: "{{today}}"
last_verified: "{{today}}"
summary: "{{one sentence a reader scans, which is the scent and not the title}}"
provenance:
  warrant: accepted
  agency: "{{human | agent | mixed}}"
  accepted_by: "{{a human, always named}}"
  evidence_basis: "{{evidenced | unevidenced}}"
relations:
  supersedes:
    - "{{the identifier of the record this one replaces, or delete this block}}"
  traces_to:
    - "{{the identifier of the evaluation that settled it, or delete this block}}"
---

# {{the title again, as the one heading of the document}}

## Context

{{The forces. What was true when this was decided, what pulled in each direction, and what made a choice necessary now. Write it in the present tense of that date, because the record is read later and never edited.}}

## Decision

{{One decision. State it as a decision: "we will …". If two decisions want this heading, write two records, because two decisions in one record cannot be superseded apart.}}

## Consequences

{{What follows, including what this forecloses and what it costs. A consequence that nobody would call good belongs here as much as one that would.}}
