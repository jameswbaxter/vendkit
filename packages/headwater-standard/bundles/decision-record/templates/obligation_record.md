---
id: "{{HW-OBL-0001, minted once and never reissued}}"
title: "{{what is owed, named as a thing and not as a question}}"
status: current
status_since: "{{today}}"
last_verified: "{{today}}"
summary: "{{one sentence a reader scans, which is the scent and not the title}}"
provenance:
  warrant: accepted
  agency: "{{human | agent | mixed}}"
  accepted_by: "{{a human, always named}}"
  evidence_basis: "{{evidenced | unevidenced}}"
relations:
  traces_to:
    - "{{the identifier of the decision that left this behind, or delete this block}}"
  discharged_by:
    - "{{the identifier of the evaluation that closed it. Absent until one runs}}"
---

# {{the title again, as the one heading of the document}}

## Context

{{Which decision or which piece of work left this behind, and what the reader has to know to see why it is owed. Name the document rather than restate it.}}

## Obligation

{{What the corpus owes. State it as a debt and not as a question, because a question belongs to a decision record that nobody has written yet.}}

## Discharge

{{What would close this, and who or what it waits on. Name the instrument if one exists, and say plainly that none does if that is the case. An obligation whose discharge nobody can state is a wish, and this heading is where that becomes visible.}}
