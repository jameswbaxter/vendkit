---
status: current
status_since: "{{today}}"
last_verified: "{{today}}"
summary: "{{what this register holds, and what it refuses}}"
doc_type: decision_register
sequence: "{{number in the series}}"
provenance:
  warrant: accepted
  agency: "{{human | agent | mixed}}"
  accepted_by: "{{a human, always named}}"
  evidence_basis: evidenced
---

# {{sequence}} — The decision register

{{What this file is: the record of decisions that the design has settled, with
the argument that settled each one. State here what it refuses, because a
register that accepts open questions becomes a backlog.}}

## {{Q1}} — {{the question, as it was asked}}

**The decision.** {{One paragraph. What holds now.}}

**Why.** {{The argument. What the alternatives were, and what each one cost.}}

**The evidence.** {{A link to the evaluation that closed it. This is the
`cites_evidence` edge, and a decision closed on no evidence says so here
instead of leaving the line out.}}

**What it left open.** {{The work the decision created. This goes to the
obligation register, and this line names it rather than holding it.}}
