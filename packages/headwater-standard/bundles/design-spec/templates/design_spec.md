---
status: draft
status_since: "{{today}}"
last_verified: "{{today}}"
summary: "{{one sentence, for the index and for routing}}"
doc_type: design_spec
sequence: "{{next number in the series}}"
provenance:
  warrant: accepted
  agency: "{{human | agent | mixed}}"
  accepted_by: "{{a human, always named}}"
  evidence_basis: "{{evidenced | reconstructed | unevidenced}}"
---

# {{sequence}} — {{title}}

{{The problem this part of the series solves, in one paragraph. A reader who
stops here should know why the document exists and what would break without it.}}

## {{The canonical arc, as guidance rather than a contract}}

The tradition converges on an order, and the order is what a reader relies on.
Delete the headings that this document does not need, and keep the ones it does
in this sequence.

- **Motivation.** What is wrong today, stated before any solution appears.
- **Vocabulary.** The terms this document uses in a way the reader may not expect.
- **The model.** The core content, which is what the rest of the document serves.
- **Process.** How the model is applied, and by whom.
- **Assurance.** How anybody knows the model holds.
- **Interfaces.** What other parts of the series depend on here.
- **Related work.** What already exists, and where this differs. Position, do not survey.
- **Limits.** What this document deliberately does not settle.

## Limits

{{What this part does not cover, and which part covers it. A series that never
says this makes every reader search the whole of it.}}
