---
status: current
status_since: "{{today}}"
last_verified: "{{today}}"
summary: "{{what this register owes, in one sentence}}"
doc_type: obligation_register
sequence: "{{number in the series}}"
provenance:
  warrant: accepted
  agency: "{{human | agent | mixed}}"
  accepted_by: "{{a human, always named}}"
  evidence_basis: evidenced
---

# {{sequence}} — Open obligations

{{What this file is: the work that settled decisions left behind. Each item
waits on something nameable — an implementation, an adopter, or a measurement.}}

## Unmeasured claims

{{One entry per claim that no run supports. Each entry names its instrument,
because a claim published without its instrument is not an honest gap.}}

- **{{The claim}}.** {{What it asserts, and the instrument that would test it.}}

## What waits on a first adopter

- **{{The item}}.** {{Why no argument settles it, and what evidence would.}}

## Design work that nothing blocks

- **{{The item}}.** {{What is missing, and what it blocks downstream.}}
