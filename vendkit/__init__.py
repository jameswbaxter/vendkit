"""VendKit — vendor curated slices of files across repositories.

Layer 0 (core) is platform-agnostic and, on the consumer PR path (gate,
migrations-verify, is-newer), standard-library only (INV-9).
"""

__version__ = "0.1.0"
